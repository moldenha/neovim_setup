# Neovim Setup

This is my neovim setup, it works well for me. If I add any more packages or configurations it will automatically be here.

## Setup:

(pretty easy to do)

```bash
// naviage to -> ~/.config
git clone --recursive github.com/moldenha/neovim_setup.git nvim
// test:
nvim nvim/init.lua
```
